export interface IUser {
  userId:string;
  imageUrl: string;
  // firstName: string;
  // lastName: string;
  // phoneNumber: string;
  // state: string;
  // city: string;
  // dateOfBirth: Date;
  // street: string;
  createdAt: Date;
  email:string;
  userName:string;
}